<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario de Pago</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <title>Formulario de Tarjeta de Crédito</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        form {
            max-width: 400px; /* Ajusta el ancho del formulario según tu preferencia */
            width: 500px;
            height: 450px;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        select {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .payBtn {
            font-family: 'Ubuntu';
            font-weight: bold;
            font-size: 18px;
            border-radius: 10px;
            background-color: #352E58;
            color: white;
            padding: 15px 25px;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <form>
        <label for="card-number">
            <i class="fas fa-credit-card"></i> Número de Tarjeta:
        </label>
        <input type="text" id="card-number" name="card-number" placeholder="Ingrese el número de tarjeta" maxlength="19" required>

        <label for="expiry-date">
            <i class="far fa-calendar-alt"></i> Fecha de Vencimiento:
        </label>
        <input type="text" id="expiry-date" name="expiry-date" placeholder="MM/AA" required>

        <label for="cvv">
            <i class="fas fa-lock"></i> CVV:
        </label>
        <input type="text" id="cvv" name="cvv" placeholder="CVV" required>

        <label for="card-holder">
            <i class="far fa-user"></i> Titular de la Tarjeta:
        </label>
        <input type="text" id="card-holder" name="card-holder" placeholder="Nombre del titular" required>

        <button class="payBtn" id="payBtn" type="submit">
            <i class="fas fa-credit-card"></i> Pagar
        </button>
    </form>

    <script>
        document.getElementById('payBtn').onclick = function () {
            window.location.href = 'ConfirmacionCompra';
        };

        function formatCardNumber(value) {
            var formatted = value.replace(/(\d{4})/g, '$1-'); // Añade guiones cada 4 dígitos
            return formatted.slice(0, -1); // Elimina el guión final si lo hay
        }
    </script>

</body>
</html>
